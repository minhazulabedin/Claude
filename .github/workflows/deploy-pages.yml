name: Deploy to GitHub Pages

on:
  push:
    branches: ["claude/automation-capabilities-5ph3oe"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages (auto-enables Pages for this repo)
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload site artifact (repo root — serves NEO//CALC at / and NEO//ROUTINE at /routine/)
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
